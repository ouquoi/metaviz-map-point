export type Settings = {
  labelColumn?: string;
  latColumn?: string;
  lonColumn?: string;
  valueColumn?: string;
  pointSize?: number;
  colorLow?: string;
  colorHigh?: string;
  showLegend?: boolean;
  legendTitle?: string;
  showTiles?: boolean;
};
